"use strict"; //treat all JS code as newer version

let name = "hitesh"
let age = 18
let isLoggedIn = false
let state;
//const uid; //canst should be initialised
 
console.log(typeof name);

console.table([typeof name, typeof age, typeof isLoggedIn, typeof state]);